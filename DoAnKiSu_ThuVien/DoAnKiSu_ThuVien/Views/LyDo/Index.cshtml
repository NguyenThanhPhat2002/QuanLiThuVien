@model IEnumerable<DoAnKiSu_ThuVien.Context.LyDo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ten_ld)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ten_ld)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_ld }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_ld }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_ld })
        </td>
    </tr>
}

</table>
