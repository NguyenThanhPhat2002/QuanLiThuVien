@model IEnumerable<DoAnKiSu_ThuVien.Context.TacGia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ten_tac_gia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ten_tac_gia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tac_gia }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tac_gia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tac_gia })
        </td>
    </tr>
}

</table>
